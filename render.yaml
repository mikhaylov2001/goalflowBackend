services:
  - type: web
    name: goalflow-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: PostgresForOldSchoolBot
          property: connectionString
